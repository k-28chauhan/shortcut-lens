"""Generates every results CSV/Markdown table (T1-T6) from run artefacts.

Concept: "no fabricated numbers" (CLAUDE.md §3 rule 5) means every number in a table must trace
back to a manifest -- this module is the only place table cells are computed, never hand-typed.

Pipeline position: oracle zone (FR-P1).

Status: stub -- implemented in M8 (see docs/PLAN.md).
"""
