"""Discovery quality: AUROC, precision@k of the top-ranked confirmed slice vs the true minority
group.

Concept: also reports the *best-matching* slice (an oracle-chosen upper reference) alongside the
top-ranked one, so the gap between "what we'd actually report" and "the best the method could
have found" is visible (FR-X2, used by E2/E4/E3).

Pipeline position: oracle zone.

Status: stub -- implemented in M4 (see docs/PLAN.md).
"""
