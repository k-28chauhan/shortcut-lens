"""Oracle-tuned hyperparameter selection: true worst-group accuracy on val_b.

Concept: reported only as a clearly labelled reference row (never mixed with label-free results)
to measure the "cost of labels" -- how much worse label-free tuning is than tuning with the
labels the whole project is trying to avoid needing (FR-M5, metric definition in PRD §12).

Pipeline position: oracle zone.

Status: stub -- implemented in M7 (see docs/PLAN.md).
"""
