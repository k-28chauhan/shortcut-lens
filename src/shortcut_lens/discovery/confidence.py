"""`confidence`: the misclassified / low-confidence set (JTT-style baseline).

Concept: the simplest possible "failure slice" -- everything the model gets wrong or is unsure
about, with no clustering at all. A baseline every smarter method should beat.

Pipeline position: label-free zone (FR-S1).

Status: stub -- implemented in M4 (see docs/PLAN.md).
"""
