"""Label-free zone: slice discovery methods, registered in `DISCOVERERS`.

Concept: `DISCOVERERS = {"confidence": ..., "error_kmeans": ..., "domino_em": ...,
"failure_direction": ...}` -- a plain dict registry (CLAUDE.md §5: no decorator-based plugin
registries).

Pipeline position: label-free zone (see docs/ARCHITECTURE.md §1).

Status: stub -- implemented in M4 (see docs/PLAN.md).
"""
