"""`error_kmeans`: k-means on class embeddings, clusters ranked by error rate.

Concept: an error-agnostic clustering method -- it groups examples by representation similarity
first, then simply checks which resulting clusters happen to have high error rates.

Pipeline position: label-free zone (FR-S2).

Status: stub -- implemented in M4 (see docs/PLAN.md).
"""
