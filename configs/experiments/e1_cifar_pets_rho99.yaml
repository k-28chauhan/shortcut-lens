# D-002/D-037 fallback: CIFAR-10 cat vs dog at rho=0.99, same erm_resnet50 train config as the
# failed planted_pets attempts, isolating "does native low resolution induce more reliance" as
# the only changed variable.
base: ../train/erm_resnet50.yaml
experiment_id: E1
dataset: planted_cifar_pets
rho: 0.99
patch_size_px: 32
patch_color: [255, 0, 255]
patch_alpha: 1.0
val_mode: balanced
build_seed: 0
